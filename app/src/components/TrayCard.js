// Card tray making function

export function genCards(pos, subProcesses) {
  console.log(pos);
  console.log(subProcesses);
  let cards = []
  let id = 'productionTrayId';
  let title = 'productionTrayName';
  return cards;
}
